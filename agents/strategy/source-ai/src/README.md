# Source code — Source AI

Potential components:

- Strategy template engine (fills sections from structured intake)
- Connectors to **market data** and **risk feeds** with caching
- Scenario scoring module (weights configurable per category policy)

Keep qualitative narrative generation separate from **deterministic** eligibility checks (sanctions, certifications).
